package com.fair.weather.core

import com.fair.weather.BuildConfig

object AppConstants {
    const val BASE_API_URL = BuildConfig.SERVER_URL
    const val WEATHER_STATE_URL = BuildConfig.WEATHER_STATE_URL
    const val WEATHER_STATE_KEY = BuildConfig.WEATHER_STATE_KEY
}